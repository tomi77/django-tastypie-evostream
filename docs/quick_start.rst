Quick Start
===========

Install package via ``pip``

::

    pip install django-tastypie-evostream

Activate ``Django`` application

::

    INSTALLED_APPS = (
        ...
        'tastypie_evostream',
        ...
    )
